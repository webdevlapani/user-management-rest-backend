// Change enum when changing constant
export const PermissionType = {
  READ: 1,
  CREATE: 2,
  DELETE: 3,
  UPDATE: 4,
};

// Change enum when changing constant
export const ModuleType = {
  USERS: 1,
  ROLES: 2,
};
